# Manager configuration for the e2e run. Short period so cycles are quick;
# common settings come from an !include to exercise borgmatic-style shared
# config files.
manager:
  period: "30m"
borgmatic:
  <<: !include common.yaml
  repositories:
    - path: /srv/borgmatic-e2e/repo
