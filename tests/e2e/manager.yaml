# Manager configuration for the e2e run. Short period so cycles are quick;
# lock_wait tolerates the passthrough subcommand racing a cycle.
manager:
  period: "15s"
borgmatic:
  repositories:
    - path: /srv/borgmatic-e2e/repo
  keep_daily: 7
  lock_wait: 30
